# Python cache files
__pycache__/
*.py[cod]
*$py.class

# Environment configuration
config.py

# Data files (keep the final training dataset for ML team handoff, but ignore intermediate raw data)
raw_reports.json
clean_reports.json

# Log files
*.log
